var senha = "Paodequeijo222"
var quantidadeCaracteres = senha.length

if (quantidadeCaracteres < 8) {
    console.log('Sua senha é fraca, recomendamos uma com 8 letras')
} else {
    console.log('Sua senha é forte, parabens')
}